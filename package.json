{
  "name": "ox-referrals",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --prod hosted-service --node https://api.thegraph.com/deploy/ --deploy-key $SUB_GRAPH_DEPLOY_KEY ox-defi/ox-subgraph",
    "create-local": "graph create --node http://localhost:8020/ ox-defi/ox-referrals",
    "remove-local": "graph remove --node http://localhost:8020/ ox-defi/ox-referrals",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ox-defi/ox-referrals"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.25.1",
    "@graphprotocol/graph-ts": "^0.20.1"
  }
}
